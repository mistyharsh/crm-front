import {
  Camera as LCamera,
  Apple as LApple,
  SquareUser as LSquareUser,
  LayoutDashboard as LLayoutDashboard,
} from 'lucide-react';

import { withIcon } from './Icon.js';

export const Apple = withIcon(LApple);
export const Camera = withIcon(LCamera);
export const LayoutDashboard = withIcon(LLayoutDashboard);
export const SquareUser = withIcon(LSquareUser);
